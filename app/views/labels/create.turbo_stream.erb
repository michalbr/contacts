<% if @label.errors.any? %>
  <%= turbo_stream.replace "modal" do %>
    <%= render "modal", title: "New Label", label: @label %>
  <% end %>
<% else %>
  <%= turbo_stream.update "modal", "" %>
  <%= turbo_stream.update "labels-list" do %>
    <%= render @labels %>
  <% end %>
<% end %>
